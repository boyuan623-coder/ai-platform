# Docker profile: 容器内使用服务名访问
spring:
  data:
    redis:
      host: redis
  cloud:
    nacos:
      discovery:
        server-addr: nacos:8848
      config:
        server-addr: nacos:8848

dubbo:
  registry:
    address: nacos://nacos:8848
